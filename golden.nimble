version = "1.1.1"
author = "disruptek"
description = "a nim benchmark"
license = "MIT"
requires "nim >= 0.20.0"
requires "cligen >= 0.9.38"
requires "foreach >= 1.0.2"

bin = @["golden"]

srcDir = "src"
